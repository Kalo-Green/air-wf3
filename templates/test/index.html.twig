{% extends 'base.html.twig' %}

{% block title %}Hello Test2Controller!{% endblock %}

{% block body %}
<table>
    <thead>
        <tr>
            <th>Couleur</th>
            <th>Code</th>
        </tr>
    </thead>
    <tbody>
        {% for ligne in liste %}
            <tr>
            {# c'est twig {{}} #}
                <td>{{ligne.nom}}</td>
                <td>{{ligne.code}}</td>

            </tr>
            {% endfor %}
    </tbody>
</table>
{% endblock %}

 {% block header %}Autre Header{% endblock %}
